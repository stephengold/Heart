plugins {
    id 'base' // To add "clean" task to the root project.
}

subprojects {
    apply from: rootProject.file('common.gradle')
}

// publishing tasks

task('install') {
    dependsOn ':HeartLibrary:install'
}
task('release') {
    dependsOn ':HeartLibrary:release'
}
